#include<stdio.h>
#include<math.h>
int main()
{
	int a,b;
	float p;
	printf("enter height and base of a triangle =");
	scanf("%d%d",&a,&b);
	p=0.5*a*b;
	printf("area of triangle = %f",p);
	return 0;
}

